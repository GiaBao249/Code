#include <bits/stdc++.h>
using namespace std;

bool UPPER(char &s)
{
    if (s >= 'A' && s <= 'Z')
        return true;
    return false;
}
bool LOWER(char &s)
{
    if (s >= 'a' && s <= 'z')
        return true;
    return false;
}

int main()
{
    string s1, s2;
    getline(cin, s1);
    getline(cin, s2);
    string res = "";
}